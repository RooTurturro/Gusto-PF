// .sequelizerc

const path = require('path'); //Permite trabajar con rutas

module.exports = {
  "config": path.resolve("./src/database/config", "config.js"), //Ruta del archivo de configuracion de la DB
  "models-path": path.resolve("./src/database/models"), //Ruta de los modelos de la DB
  "seeders-path": path.resolve("./src/database/seeders"), //Ruta de los seeders de la DB
  "migrations-path": path.resolve("./src/database/migrations"), //Ruta de los migrations de la DB
};